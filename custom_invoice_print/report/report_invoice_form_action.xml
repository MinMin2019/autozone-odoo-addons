<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Action: พิมพ์ใบกำกับภาษีลงฟอร์มสำเร็จรูป (dot-matrix) -->
        <record id="action_report_invoice_form" model="ir.actions.report">
            <field name="name">ใบกำกับภาษี</field>
            <field name="model">account.move</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">custom_invoice_print.report_invoice_form</field>
            <field name="report_file">custom_invoice_print.report_invoice_form</field>
            <field name="print_report_name">(object._get_report_base_filename())</field>
            <field name="paperformat_id" ref="custom_invoice_print.paperformat_invoice_form" />
            <field name="binding_model_id" ref="account.model_account_move" />
            <field name="binding_type">report</field>
        </record>
    </data>
</odoo>
