<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_is_vendor" model="ir.ui.view">
        <field name="name">res.partner.form.is.vendor</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='sales_purchases']//group[@name='purchase']" position="inside">
                <field name="is_vendor"/>
            </xpath>
        </field>
    </record>

    <record id="view_res_partner_filter_is_vendor" model="ir.ui.view">
        <field name="name">res.partner.select.is.vendor</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <filter name="inactive" position="before">
                <filter string="Vendors" name="filter_is_vendor" domain="[('is_vendor', '=', True)]"/>
            </filter>
        </field>
    </record>
</odoo>
