<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_az_attendance_sync" model="ir.cron">
            <field name="name">ดึงเวลาตอกบัตรจากเครื่องสแกน (BpControl)</field>
            <field name="model_id" ref="model_az_attendance_sync"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="False"/>
            <field name="nextcall" eval="(DateTime.now().replace(hour=22, minute=30, second=0) + relativedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>

    </data>
</odoo>
